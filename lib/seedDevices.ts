/**
 * Seed Devices Data
 * 
 * This file contains the saved devices configuration with positions.
 * Update this file with your exported devices data to preserve them across deployments.
 * 
 * To update:
 * 1. Run exportFusionData() in browser console
 * 2. Copy the devices array from the output
 * 3. Replace the devices array below
 * 4. Commit to repository
 */

import { Device } from './mockData'

// If you have saved devices, replace this array with your exported data
// Otherwise, this will be null and the app will use mockData.ts
export const seedDevices: Device[] | null = null

