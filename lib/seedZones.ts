/**
 * Seed Zones Data
 * 
 * This file contains the saved zones configuration.
 * Update this file with your exported zones data to preserve them across deployments.
 * 
 * To update:
 * 1. Run exportFusionData() in browser console
 * 2. Copy the zones array from the output
 * 3. Replace the zones array below
 * 4. Commit to repository
 */

import { Zone } from './stores/zoneStore'

// If you have saved zones, replace this array with your exported data
// Otherwise, this will be null and the app will use initialZones.ts
export const seedZones: Zone[] | null = null

